TO RUN TESTS YOU MUST FIRST:
change tests/config.py to point to the /tests/content and /tests/users directories
> pip install -r requirements.txt
    #note requirements has been updated to include pytest


Then the testing suite can be run with the command:
> pytest